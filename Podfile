# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SampleAssesement' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  	pod 'SDWebImage', '~> 4.0'
	pod 'Masonry'

  target 'SampleAssesementTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SampleAssesementUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
